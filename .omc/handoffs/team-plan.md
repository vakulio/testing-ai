## Handoff: team-plan → team-exec
- **Decided**: Stack is Angular 22 + Tailwind 4. User chose to INSTALL Angular Material (not MUI/React). Foundation done by lead: `@angular/material`+`@angular/cdk`+`@angular/animations` v22 installed; M3 palettes generated from brand teal (primary) + coral (tertiary) → `src/_theme-colors.scss`; theme wired in `src/theme.scss` (mat.theme + brand token overrides for pill buttons/16px cards/mono chips) and added to angular.json styles; `provideAnimationsAsync()` added to app.config.ts; fonts swapped in index.html (Space Grotesk display / Inter body / JetBrains Mono utility / Material Icons); Tailwind `@theme` updated (font-display, font-mono, brand-ink). Dev build is GREEN.
- **Design direction**: keep the distinctive brand (teal/coral/gold/mint) — it is NOT a generic AI default. Signature = "syntax-token" accent: mono `// eyebrows` + one tokenized keyword in the hero. Full spec in `docs/DESIGN.md`.
- **Rejected**: Angular Material default look (Roboto/purple) — override tokens instead. Editing global styles from workers — lead owns `src/styles.css` + `src/theme.scss`. Changing component public inputs/selectors/routes — forbidden (pages depend on them).
- **Risks**: Tailwind 4 preflight vs Material component styles (watch buttons/inputs during verify). mat-icon uses Material Icons classic font (ligature names). Dev bundle 1.37MB (Material weight; prod tree-shakes).
- **Files**: docs/DESIGN.md, src/theme.scss, src/_theme-colors.scss, src/styles.css, src/index.html, src/app/app.config.ts, angular.json.
- **Remaining (team-exec)**: 4 parallel designer workers — chrome (header/footer/sidebar), cards (course/tutorial/testimonial/star-rating), blog pages (home/tutorials/tutorial-detail), courses pages (courses/course-detail/recommendations/legal). Then lead runs Playwright verify per page → fix loop.
